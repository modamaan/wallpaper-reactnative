import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const suggested = () => {
  return (
    <SafeAreaView>
      <Text>suggest page</Text>
    </SafeAreaView>
  );
};

export default suggested;
